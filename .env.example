# Optional: AI fix suggestions (--with-ai). Prefer Groq (used by src/ai/suggestions.ts).
# https://console.groq.com — never commit real keys; use CI secrets for pipelines.
GROQ_API_KEY=

# Legacy fallback name only: same env is read if GROQ_API_KEY is unset (see suggestions.ts).
GEMINI_API_KEY=
